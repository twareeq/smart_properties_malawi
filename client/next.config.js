/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['your-supabase-project.supabase.co', 'images.unsplash.com', 'localhost'],
  },
};

module.exports = nextConfig;
